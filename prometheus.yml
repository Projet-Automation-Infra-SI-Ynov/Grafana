global:
  scrape_interval:     15s
  evaluation_interval: 15s
  scrape_timeout:      10s

# Alertmanager configuration
alerting:
  alertmanagers:
  - static_configs:
    - targets:
      # - alertmanager:9093

# Load rules once and periodically evaluate them according to the global 'evaluation_interval'.
rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

# A scrape configuration containing exactly one endpoint to scrape:
# Here it's Prometheus itself.
scrape_configs:
  - job_name: 'prometheus'
    static_configs:
    - targets: ['prometheus:9090']
  # Jenkins
  - job_name: 'jenkins'
    metrics_path: /prometheus/
    static_configs:
    - targets: ['IP_JENKINS:80']
  - job_name: 'jenkins2'
    static_configs:
    - targets: ['IP_JENKINS:9100', 'IP_JENKINS:9323']
  # Registry
  - job_name: 'registry'
    static_configs:
    - targets: ['IP_REGISTRY:5000', 'IP_REGISTRY:9100', 'IP_REGISTRY:9323']